# 完整现有项目发布检查 · 2026-09-18

本轮授权：将完整埃及项目发布到既有 GitHub Pages 站点，不再发布为单景点。目标保持 `https://dawnkuo.github.io/nmec-cultural-guide/`；首页、行程、城市、凭证状态和全部景点均进入发布目录，旧 NMEC 深链接保留。

## 发布范围

12 日行程、4 个城市文化章节、14 个景点、106 条解读、28 个室内／场地层级、18 个外观模型，23 条页面路由。按当前导览目录生成，而不是单馆清单。没有修改欧洲行项目，也没有将父工作区作为 Git 仓库推送。

## 本轮变更与检查边界

- 迁入完整项目的当前内容、地图和资源，统一 GitHub Pages base path、内部导航、静态深链接、图片和地图数据请求。
- 保留上次发布修正过的 NMEC 媒体身份与署名，沿用独立作用域、按文件校验、断点续传和旧完整版本回退的 Service Worker。
- Commons API 重新核对 103 条媒体作者、原始页面和许可证；吉萨三张缺精确文件名的照片使用实际缩略图 SHA-256 比对主体来源，河谷神庙对应《Valley Temple of Khafre 卡夫勒谷廟 - panoramio.jpg》，而不是同名的另一张照片。原来将金字塔石芯近景标为王后金字塔的照片改为已有、可见附属金字塔的吉萨全景。
- 阿斯旺城市页不再使用木乃伊照片，四个城市页改用对应地区已核对的景点封面。
- 桌面全部封面、28 个图层的 2D／3D、18 个外观模型的截图已逐张查阅；窄屏截图检查长标题、媒体裁切和地图呈现。封面主体覆盖 NMEC 正门、吉萨高地、GEM 入口、阿里清真寺、哈利利市场、阿布辛贝、康翁波、埃德富、卢克索、帝王谷、哈特谢普苏特、门农巨像、卡纳克及 Orange Bay。
- 本轮为现有完整项目的发布与运行回归，不是重新考证所有史实、藏品身份或空间几何。未把旧单馆验收结果复制成其余 13 个景点的内容验收，也不声称博物馆精选章节等于完整馆藏覆盖。`guide-build-manifest.json` 将发布验证与尚未重新核验的内容／模型验收明确分开。

## 可复核检查

`docs/qa/full-local-report.json`：三种尺寸 1440×1000、1094×768、390×844 的全部 23 路由；图片解码、base path、横向溢出、全部图层最小／最大缩放的精确标注 ID／文字、首中尾空间选择、双视图、外观选择与视角、搜索、图片弹窗、现场模式。全部 23 路由离线打开，景点离线加载内外 3D。手机为 Chrome 仿真，不是真机多点触控验收。

`docs/qa/full-fallback-report.json`：14 个景点在模拟 WebGL 不可用时保留选择并回退同模型 2D；不是实际 GPU 故障测试。

`docs/qa/full-resume-report.json`：真实 Service Worker 的网络中断、页面关闭／重开、保留已校验资源、续传、部分更新保留旧完整包、更新完成回收旧版本及不影响其他项目缓存。

生产构建有主包和 Three.js 大于 500 kB 的体积警告，非构建错误。资源使用本地离线包，浏览器清理存储后需要重新下载。

三张图片的 Commons API 作者字段为空，已另行查阅原始文件页补齐：Abu Simbel 06 为 Hajor 于 2002 年 12 月拍摄的搬迁模型照片（CC BY-SA 3.0）；Hatshepsut 02／03 为 Wellcome Library, London / Wellcome Images 提供的历史复原图（CC BY 4.0），不是现代实景照片或公版素材。API 元数据核对不等于所有正文配图都完成了重新逐对象鉴定。

发布后的全部页面复测单独记录于 `docs/qa/full-live-report.json`，不以推送成功代替线上可用性。
